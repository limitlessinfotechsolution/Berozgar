# Normalise to LF in the repository. Windows checkouts still get CRLF locally,
# but CI (ubuntu) and Windows see the same bytes, so no whole-repo diffs.
* text=auto eol=lf

# Binary — never touch.
*.ico binary
*.png binary
*.jpg binary
*.webp binary
*.woff binary
*.woff2 binary
