# Berozgar — Storefront

The customer-facing website for Berozgar: catalogue, bag, checkout and order tracking.

Built with Next.js 16 (App Router), React 19 and Tailwind v4. The catalogue and orders are
served by the Berozgar ERP, which runs as a separate project — see
[Relationship to the ERP](#relationship-to-the-erp).

## Requirements

- Node 24 (see `.nvmrc`)
- npm

## Getting started

```bash
npm install
cp .env.local.example .env.local   # then fill in the values
npm run dev
```

The site runs on **http://localhost:3003**. Port 3000 is deliberately left to the ERP admin.

### Environment

All three variables are documented in `.env.local.example`:

| Variable | Purpose |
|---|---|
| `NEXT_PUBLIC_SITE_URL` | Public origin. Used by the sitemap, `robots.txt`, canonical URLs and OG images. A **production build fails without it**; dev falls back to `:3003`. |
| `ERP_API_URL` | Base URL of the ERP's public API (`apps/api`, `:3001` locally). Read at **request** time — checkout, quote and tracking all call the ERP per request — so a deploy needs it in the host's runtime environment. |
| `NEXT_PUBLIC_ERP_API_URL` | Deploy-time alternative to the above, for hosts whose runtime environment is awkward to set (this is how Netlify is configured). `NEXT_PUBLIC_*` is inlined into the bundle at build, server code included. `ERP_API_URL` wins when both exist. Never use a `NEXT_PUBLIC_` name for a secret — it ships in client JS. |
| `REVALIDATE_SECRET` | Shared secret for `/api/revalidate`. Must equal `STOREFRONT_REVALIDATE_SECRET` in the ERP admin's `.env`. |

`.env.local` is git-ignored. Never commit real secrets.

## Scripts

| Command | Does |
|---|---|
| `npm run dev` | Dev server on port 3003 |
| `npm run build` | Production build |
| `npm start` | Serve the production build on port 3003 |
| `npm run lint` | ESLint |
| `npm test` | Vitest, single run |

## Relationship to the ERP

The storefront owns presentation only. Product data, pricing, GST and order creation live in
the ERP; the site reads them over the ERP's public API and never computes a price it sends to
the server.

- **Catalogue** — read from `GET /api/public/v1/products`, cached under the `catalogue` tag.
  Admin edits call back into `POST /api/revalidate` here, so changes appear within seconds; a
  60 s revalidate window means a missed webhook self-heals.
- **Checkout** — `/api/checkout` is a server-side proxy to the ERP, which prices the bag and
  creates the order. There is no client-supplied total and no fallback order id.
- **Tracking** — `/track-order` reads order status from the ERP by order number + phone.

Customer accounts, online payment capture, coupons and reviews are still local to the
storefront. The full contract, including what is built and what is still open, is in
[`docs/INTEGRATION.md`](docs/INTEGRATION.md).

## Project layout

```
src/app/         routes (App Router) — shop, cart, checkout, account, legal, journal
src/components/  client components and providers (cart, catalogue, session)
src/lib/         data access, catalogue mapping, search, tracking — the tested code
src/content/     legal copy fallback, used when the ERP is unreachable
docs/            integration contract and brand/design briefs
```

## Notes

- `AGENTS.md` is generated by `next dev` and is committed on purpose — leaving it uncommitted
  just recreates it as a dirty file.
- Tests cover `src/lib` (catalogue mapping, tracking, site config). Run them with `npm test`.
